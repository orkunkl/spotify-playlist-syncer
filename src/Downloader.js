'use strict';

var youtubedl = require('youtube-dl');


module.exports.main = async (event, context) => {


	return {
   		statusCode: statusCode,
   		body: JSON.stringify({
	    message: "downloader"
   	}),
}
